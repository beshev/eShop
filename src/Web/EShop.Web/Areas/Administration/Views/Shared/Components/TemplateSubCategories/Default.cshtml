@using EShop.Web.ViewModels.Products
@model IEnumerable<SelectViewModel>

<div class="form-group">
    <label>Под категории</label>
    <select name="SubCategoryId"
            class="form-select m-1"
            asp-items="@(new SelectList(Model, nameof(SelectViewModel.Id), nameof(SelectViewModel.Name), this.ViewData[GlobalConstants.NameOfCategory]))">
            <option value="">Няма</option>
    </select>
</div>